{
  "name": "uw-lib-mpan.js",
  "version": "0.0.0",
  "description": "Simple MPAN parsing and validation library",
  "main": "index.js",
  "scripts": {
    "compile": "babel -d ./dist ./lib",
    "test": "mocha -r babel-polyfill -r tests/.bootstrap.js --compilers js:babel-core/register tests"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "chai": "^3.5.0",
    "chai-things": "^0.2.0",
    "mocha": "^3.0.2"
  }
}
